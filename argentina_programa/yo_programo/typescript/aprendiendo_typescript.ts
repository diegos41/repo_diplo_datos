let a: number; //Declarada explícitamente
let b: string; //Definida explícitamente
let c=101;     //Declarada implícitamente. Typescript infiere el tipo.
let recursos:string[]= ['memoria', 'disco', 'procesador'];  //Declarada explícitamente

let message: string= 'Hello World';
console.log(message);

